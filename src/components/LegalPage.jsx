import React from 'react';

export default function LegalPage({ eyebrow, title, effective, updated, intro, sections }) {
  return <main><header className="border-b bg-card/60"><div className="mx-auto max-w-4xl px-4 py-16 sm:px-6"><p className="text-xs font-bold uppercase tracking-[.18em] text-primary">{eyebrow}</p><h1 className="mt-3 font-heading text-4xl font-black tracking-tight sm:text-5xl">{title}</h1><p className="mt-4 text-sm text-muted-foreground">{effective && <>Effective date: {effective} <span className="mx-2" aria-hidden="true">|</span> </>}Last updated: {updated}</p>{intro && <p className="mt-6 max-w-3xl text-sm leading-7 text-muted-foreground">{intro}</p>}</div></header><article className="mx-auto max-w-4xl space-y-10 px-4 py-16 sm:px-6">{sections.map(({ heading, paragraphs, items }) => <section key={heading}><h2 className="font-heading text-2xl font-bold">{heading}</h2>{paragraphs?.map((paragraph) => <p key={paragraph} className="mt-4 text-sm leading-7 text-muted-foreground">{paragraph}</p>)}{items && <ul className="mt-4 list-disc space-y-2 pl-5 text-sm leading-7 text-muted-foreground">{items.map((item) => <li key={item}>{item}</li>)}</ul>}</section>)}</article></main>;
}
